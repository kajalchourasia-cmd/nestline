# Nestline current engineering status

**Baseline:** Kajal repository `main` at `796c05cd29a055188e86685990078709fca0bfbc` (merged PR #4).
**Improvement branch:** `fix/consolidated-stages-0-8-and-10-improvements`.
**Scope:** Stages 0–8 and 10 services, governance, configuration and verification. Stage 9 UI and deployment are unchanged.

Stages 5–10 are present on `main`. The presently verified end-to-end product path is the deterministic fictional Demo Mode. Personal Mode storage and permission paths have local Supabase evidence, but Personal Mode is not a complete live maternal-health assistant because public content remains unpublished, the safety specification is draft, no live generation provider is selected, real uploads are disabled and external human review/deployment gates remain open.

Current governed inventory: 63 authoring profiles, 31 sources, 55 source spans and 56 guidance fragments. Published profiles: **0/63**. Display-eligible fetal comparisons: **0/42**. PC00, P10 and PP01 have recorded Kajal content/product decisions only; specialist and release decisions are incomplete.

The provider-neutral transport is implemented. A frozen fictional run measured xAI `grok-4.6` at 6/8 completed cases (4/8 Stage 8 displays; $0.020862) and therefore did not select it. OpenAI `gpt-5.4-mini` model-list access succeeded, but inference returned HTTP 429/no-credit and completed only the two zero-call safety cases. No provider is selected.

Controlled safety engineering stays fail-closed. The expanded development set covers all five entry channels and adversarial language, but the specification remains draft and the reduced-movement policy gap requires qualified review. Passing software tests do not establish clinical validation.

Remote migrations, hosting, deployment, public publication and real-patient use remain unauthorized and pending.
